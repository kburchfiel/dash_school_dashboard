# Based in part on: https://dash.plotly.com/urls
import dash

from dash import html, dcc, callback, Input, Output
import pandas as pd
import plotly.express as px

dash.register_page(__name__)


layout = html.Div([
html.H1(children='Content forthcoming!')
])
